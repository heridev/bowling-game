require 'rspec'
require_relative '../game'

describe Game do
  #let(:frame){ mock 'Frame'}
  #let(:frames_score){
    #[0,10,25,25,10]
  #}
  #subject.should_receive(:score).with(frame_score)
  ##frame.should_receive(:generate_frames).with(:frame_score).and_return true
  #frame.should_receive(:score).with(frame_score).and_return true
  #it 'should return a total score' do
    #subject.score.should eq(70)
  #end

  it '#print_total_score' do
    #let(:total_score) { '100' }
    #let(:message) { 'algo' }
    ##subject.should_receive(:calculate_score).with(100).and_return true
    #subject.print_total_score.should eq(message)
  end

end
